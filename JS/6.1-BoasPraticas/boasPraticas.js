//evitar declarar variáveis no escopo global
//o idel será declarar numa função auto executada
// quando for declarar varias variáveis não precisar digitar var sempre basta colocar vírgula entre as variáveis
//evitar o uso do hiring
//já declare a variável no topo do escopo
//Strings sempre usar com aspas duplas em vez de aspas simples. "casa"
//procurar colocar os códigos relacionados ao mesmo assunto num mesmo parágrafo ou próximos entre si
//sempre separar os assuntos por um enter


(function(){
	var escopolocal,
		foo,
		pat,
		obj,
		array;

	escopolocal = "enter";

	//var obj = {foo: "bar", bar: "foo", foobar: "oi"} lembrar de não declarar uma variável no meio do código
	//se tiver muitos parâmetros o ideal é quebrar as linhas. O mesmo para arrays e em declaração de variáveis.
	obj = {
		foo: "bar", //deixar sem espaço na frente de cada declaração
		bar: "foo",
		foobar: "oi"
	}
})();





















